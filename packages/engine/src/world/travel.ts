import type { SaveGame } from '../world/state.js';
import type { TickContext } from '../simulation/tick.js';

/** Personal travel lands with US5 (T098). */
export function resolveTravelTick(_save: SaveGame, _ctx: TickContext): void {}
