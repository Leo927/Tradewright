import type { SaveGame } from '../world/state.js';
import type { TickContext } from '../simulation/tick.js';

/** Shipment lifecycle lands with US5 (T096). */
export function resolveArrivals(_save: SaveGame, _ctx: TickContext): void {}
