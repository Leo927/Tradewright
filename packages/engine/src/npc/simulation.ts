import type { SaveGame } from '../world/state.js';
import type { TickContext } from '../simulation/tick.js';

/** NPC market evolution lands with US4 (T087). */
export function runNpcTick(_save: SaveGame, _ctx: TickContext): void {}
