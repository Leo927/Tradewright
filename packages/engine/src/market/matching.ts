import type { SaveGame } from '../world/state.js';
import type { TickContext } from '../simulation/tick.js';

/** Order expiry + matching on tick lands with US4 (T086). */
export function runMarketTick(_save: SaveGame, _ctx: TickContext): void {}
